$(function() {
  $('.typed').typed({
    strings: ["タカハシタカユキのポートフォリオ"],
    // strings: ["タカハシタカユキのポートフォリオ", "左の画像をクリック"],
    typeSpeed: 100,
    startDelay: 1000,
    backSpeed: 20,
    loop: false,
    loopCount: Infinity,
    showCursor: false,
    backDelay: 500
  });
});
